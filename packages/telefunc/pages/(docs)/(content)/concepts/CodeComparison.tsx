export const CodeComparison = ({ children }: { children: [React.ReactNode, React.ReactNode] }) => {
  return <div className="landing-code-samples grid grid-cols-2 gap-4 items-stretch">{children}</div>
}
