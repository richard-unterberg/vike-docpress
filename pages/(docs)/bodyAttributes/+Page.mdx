import { Link } from '@/components/Link'
import { ConfigSpec } from '@/components/ConfigSpec'

<ConfigSpec
  env="server"
  cumulative
  global
/>

The `bodyAttributes` setting adds attributes to the `<body>` tag.

```ts
// pages/+config.ts

import type { Config } from 'vike/types'

export default {
  // <body class="dark">
  bodyAttributes: { class: 'dark' }
} satisfies Config
```


## See also

- <Link href="/head-tags" />
- <Link href="/settings#html-shell" doNotInferSectionTitle />